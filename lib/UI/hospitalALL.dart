import 'package:flutter/material.dart';

class hospitalAll extends StatelessWidget {
  const hospitalAll({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
